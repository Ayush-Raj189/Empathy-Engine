# Copy this file to `.env` and edit values as needed.
# The app runs without this file; these are optional runtime overrides.

# Where generated audio files are saved.
EMPATHY_OUTPUT_DIR=audio_output

# TTS language code used by the pipeline (for gTTS/pyttsx3 flow).
EMPATHY_LANG=en
